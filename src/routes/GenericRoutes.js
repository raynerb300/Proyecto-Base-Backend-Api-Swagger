import { Router } from 'express'
    // Los metodos del controlador que 
//import { createGeneric, updateGeneric, getGeneric, getPGeneric, deleteGeneric } from '../controllers/ControllerGeneric.js'
const router = Router();
    // Rutas 
// router.get('/generic', getGeneric);
// router.post('/generic', createGeneric);
// router.put('/generic/:id', updateGeneric);
// router.get('/generic/:id', getGeneric);

export default router;